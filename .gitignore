# Distrobox ignore
.sudo_as_admin_successful
.wget-hsts
/.cache
/.config
/.pki
/.vscode
/.ssh
/.local
.bash_history
/.dotnet
/.gitconfig
/.profile
/.bash_logout

# Claude
.claude.json
/.claude

# Project repo (separate, independent git repo — never tracked from dev-env)
/dominion-grpc/